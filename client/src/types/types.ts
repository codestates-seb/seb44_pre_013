import { IconDefinition } from '@fortawesome/fontawesome-svg-core';

export interface IDataType {
  icon?: IconDefinition;
  size?: string;
  subtitle?: string;
  content?: string;
  subContent?: string;
}
