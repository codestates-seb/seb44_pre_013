import { IconDefinition } from '@fortawesome/fontawesome-svg-core';

export interface DataType {
  icon?: IconDefinition;
  size?: string;
  postId?: number;
  subtitle?: string;
  content?: string;
  subContent?: string;
}
